algoritmo{
    variaveis{
        inteiro k;
        flut j, t;
        cadeia cad;
    }
    {
        k = 10;
        j = 2.5;
        t = k + j;
        escreva("A soma de %i com %f é igual a %f, %s", k, j, t);//Variaveis em excesso no print
        escreval("O numero inteiro da soma e %i", k);
    }
}